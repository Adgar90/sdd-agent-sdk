# SDD Workflow Adapter

This workspace uses a neutral SDD core under `.sdd/`. Kiro may be used as an interface, but `.sdd/` remains canonical. Do not duplicate specs into Kiro-native structures unless explicitly requested.
