# Steering

Steering files contain the current stable context that applies broadly across the workspace.

Rules:
- Keep steering small and curated.
- Prefer updating existing steering files over creating new ones.
- Store historical rationale in `.sdd/decisions/`.
- Store executable work in `.sdd/specs/`.
- Store task-specific findings in `notes.md`.
